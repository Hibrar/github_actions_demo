name: GitHub CUSTOM Actions
on: workflow_dispatch

jobs:
  on-push-job:
    runs-on: ubuntu-latest

    steps:
      - name: First Step
        uses: actions/checkout@v4
      - name: Install Tree
        uses: apt install tree -y
      - name: Investigate Repo
        run: |
          pwd
          tree